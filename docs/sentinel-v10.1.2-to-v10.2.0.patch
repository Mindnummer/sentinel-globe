--- /mnt/data/sentinel-globe-v10.2.0/archive/index-v10.1.2-stable.html	2026-07-13 17:32:47.173151518 +0000
+++ /mnt/data/sentinel-globe-v10.2.0/index.html	2026-07-13 17:41:23.166245985 +0000
@@ -210,6 +210,20 @@
 #mobileNav button{flex:1;background:none;border:none;color:#9fb8ae;font:11px inherit;cursor:pointer}
 #mobileNav button.on{color:var(--vir3,#7fe0bd);background:#12261e}
 body.focus #mobileNav{display:none!important}
+/* ---------- Weather Core ---------- */
+#radarBar{max-width:min(96vw,980px);flex-wrap:wrap;justify-content:center}
+#radarScrub{width:min(280px,28vw);accent-color:var(--vir2)}
+#radarSpeed,#radarSpan{background:var(--panel2);border:1px solid var(--line);color:var(--txt);border-radius:7px;padding:4px}
+.wxgrid{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:7px;margin:6px 0}
+.wxcard{background:#081510;border:1px solid var(--line);border-radius:9px;padding:8px}
+.wxcard .big{font:600 20px/1.2 var(--mono);color:var(--vir3);margin-top:2px}
+.wxbar{display:flex;gap:5px;align-items:center;flex-wrap:wrap;margin:5px 0}
+.wxbar select{background:var(--panel2);border:1px solid var(--line);color:var(--txt);border-radius:7px;padding:5px}
+.wxlegend{display:flex;gap:10px;flex-wrap:wrap;font-size:10px;color:var(--dim);margin:4px 0}
+.wxlegend span::before{content:"";display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:4px;background:var(--c)}
+body.wxmode #btnWeather{background:#58c7e8;color:#061217;border-color:#8de6ff;font-weight:700}
+body.wxmode #mapWrap{box-shadow:inset 0 0 0 1px #58c7e855}
+@media(max-width:900px){#radarBar{bottom:52px;padding:5px 7px;gap:5px}#radarScrub{width:42vw}.wxgrid{grid-template-columns:1fr 1fr}}
 </style>
 </head>
 <body>
@@ -224,6 +238,7 @@
   </div>
   <button class="hbtn" id="btnHelp" title="What am I looking at? — explainer index">?</button>
   <button class="hbtn" id="btnNotif" title="Browser notifications for your home watch zone">🔔</button>
+  <button class="hbtn" id="btnWeather" title="Weather Core — radar, lightning, alerts and forecast">☁ WX</button>
   <button class="hbtn" id="btnFocus" title="Hide all panels — map only (great with Ambient)">◱ FOCUS</button>
   <button class="hbtn" id="btnFullscreen" title="True fullscreen (Esc to exit)">⛶</button>
   <button class="hbtn" id="btnAmbient" title="Slow beautiful auto-rotation — leave it running">◉ ORBIT</button>
@@ -272,8 +287,9 @@
     <label class="satrow"><input type="checkbox" data-sat="cosmos-2251-debris"> Debris: Cosmos-2251 <span class="finer">(first 400)</span></label>
     <div class="finer" id="satNote">SGP4-propagated from CelesTrak orbits — MODELED positions from MEASURED orbits. Not a video feed; none exists publicly.</div>
   </div>
-  <h2>Radar Loop <button class="ibtn" data-info="radar">i</button></h2>
-  <button class="subbtn" id="btnRadarPlay">▶ Play last 50 min</button>
+  <h2>Weather Core <button class="ibtn" data-info="weathercore">i</button></h2>
+  <button class="subbtn" id="btnRadarPlay">▶ Open fluid radar timeline</button>
+  <div id="lightningMini" class="finer">Lightning points require a Weatherbit key; NOAA GLM viewer works without one.</div>
   <h2>Power Infrastructure <button class="ibtn" data-info="power">i</button></h2>
   <button class="subbtn" id="btnScanPower">⚡ Scan plants / substations / lines in view</button>
   <div class="finer" id="powerNote">Zoom to at least county level, then scan. Source: OpenStreetMap (ODbL). Only public map data — never private topology.</div>
@@ -290,7 +306,10 @@
 <div id="mapWrap">
   <div id="map"></div>
   <div id="mockBanner">⚠ MOCK MODE — SAMPLE DATA IS DISPLAYED AND LABELED MOCK</div>
-  <div id="radarBar"><span>NEXRAD</span><button class="hbtn" id="btnRadarPause">⏸</button>
+  <div id="radarBar"><span>NEXRAD</span>
+    <button class="hbtn" id="radarBack" title="previous frame">◀</button><button class="hbtn" id="btnRadarPause">▶</button><button class="hbtn" id="radarNext" title="next frame">▶|</button>
+    <input type="range" id="radarScrub" min="0" max="10" step="1" value="10" title="scrub radar timeline">
+    <select id="radarSpeed" title="playback speed"><option value="1100">0.5×</option><option value="650" selected>1×</option><option value="350">2×</option></select>
     <select id="radarSpan" title="loop length — beyond 50 min uses the IEM archive (experimental)">
       <option value="50">50 min · live</option><option value="180">3 h · archive</option>
       <option value="720">12 h · archive</option><option value="1440">24 h · archive</option>
@@ -310,6 +329,7 @@
 <div class="panel" id="bottom">
   <div id="tabs">
     <div class="tab on" data-tab="alerts">ALERTS</div>
+    <div class="tab" data-tab="weather">WEATHER</div>
     <div class="tab" data-tab="grid">GRID</div>
     <div class="tab" data-tab="ledger">TRUTH LEDGER <button class="ibtn" data-info="ledger">i</button></div>
     <div class="tab" data-tab="watchdog">WATCHDOG <button class="ibtn" data-info="watchdog">i</button></div>
@@ -326,12 +346,12 @@
 <div id="modalBack"><div id="modal"></div></div>
 <script>
 /* ===== SINGLE SOURCE OF TRUTH for the build version — bump ONLY here ===== */
-const APP_VERSION="10.1.2";
+const APP_VERSION="10.2.0";
 document.title="Sentinel Globe v"+APP_VERSION+" — Public Telemetry";
 document.addEventListener("DOMContentLoaded",()=>{const v=document.getElementById("verTag");
   if(v)v.textContent="v"+APP_VERSION+" · PUBLIC TELEMETRY";});
 /* =====================================================================
-   SENTINEL GLOBE v10 — core
+   SENTINEL GLOBE v10.2 — Weather Core
    Integrity law: every value carries {source, timestamp, confidence}.
    Vocabulary: MEASURED | OFFICIAL | MODELED | MOCK | EXPERIMENTAL | UNAVAILABLE
    ===================================================================== */
@@ -351,7 +371,9 @@
 const savedPerf=store.get("perf","auto");
 const cfg={
   firmsKey:store.get("firmsKey",""), eiaKey:store.get("eiaKey",""), nasaKey:store.get("nasaKey","DEMO_KEY"),
-  tomtomKey:store.get("tomtomKey",""), aisKey:store.get("aisKey",""),
+  tomtomKey:store.get("tomtomKey",""), aisKey:store.get("aisKey",""), weatherbitKey:store.get("weatherbitKey",""),
+  lightningRadius:Math.max(10,Math.min(75,Number(store.get("lightningRadius",50))||50)),
+  lightningMinutes:Math.max(10,Math.min(45,Number(store.get("lightningMinutes",30))||30)),
   showAlt:store.get("showAlt",true), enrich:store.get("enrich",true),
   autoStreet:store.get("autoStreet",true), clutter:store.get("clutter",true),
   home:safeHome,
@@ -362,12 +384,16 @@
 $("orbitSpeed").value=String(cfg.orbitSpeed);
 $("orbitSpeedV").textContent=cfg.orbitSpeed.toFixed(1).replace(".0","")+"×";
 const S={loc:cfg.home, mock:false, integrity:false, ambient:false,
-         quakes:[], fires:[], aircraft:[], alerts:[], donki:[], ba:{}, power:null,
+         quakes:[], fires:[], lightning:[], lightningMeta:null, aircraft:[], alerts:[], donki:[], ba:{}, power:null,
          ships:{}, sats:[], flows:[], issTrack:[], issRec:null,
          kp:null, wind:null, xray:null, iss:null, issTrail:[], meteo:null, anomalies:[]};
 const haversine=(a,b,c,d)=>{const R=6371,r=x=>x*Math.PI/180;
   const h=Math.sin(r(c-a)/2)**2+Math.cos(r(a))*Math.cos(r(c))*Math.sin(r(d-b)/2)**2;
   return 2*R*Math.asin(Math.sqrt(h));};
+function radiusPolygon(lat,lon,km){const R=6371,la=lat*Math.PI/180,lo=lon*Math.PI/180,ang=km/R,pts=[];
+  for(let b=0;b<=360;b+=5){const br=b*Math.PI/180;const la2=Math.asin(Math.sin(la)*Math.cos(ang)+Math.cos(la)*Math.sin(ang)*Math.cos(br));
+    const lo2=lo+Math.atan2(Math.sin(br)*Math.sin(ang)*Math.cos(la),Math.cos(ang)-Math.sin(la)*Math.sin(la2));pts.push([lo2*180/Math.PI,la2*180/Math.PI]);}
+  return{type:"Feature",properties:{km},geometry:{type:"Polygon",coordinates:[pts]}};}
 
 /* =====================================================================
    TRUTH LEDGER — chain of evidence for every fetch and alert
@@ -393,7 +419,7 @@
     wd(id, age>a.staleSec?"warn":"ok", age>a.staleSec?`stale ${fmtAge(a.lastOk)} (limit ${Math.round(a.staleSec/60)}m)`:`fresh · ${fmtAge(a.lastOk)}`);
   }
   // mock-data guard: RED if any mock item visible while in live mode
-  const leak=!S.mock&&(S.quakes.some(q=>q.mock)||S.fires.some(f=>f.mock)||S.alerts.some(a=>a.mock));
+  const leak=!S.mock&&(S.quakes.some(q=>q.mock)||S.fires.some(f=>f.mock)||S.lightning.some(x=>x.mock)||S.alerts.some(a=>a.mock));
   wd("mockguard",leak?"bad":"ok",leak?"MOCK DATA VISIBLE IN LIVE MODE — investigate":"no mock data in live views");
   wd("clock",Math.abs(new Date().getTimezoneOffset())<900?"ok":"warn","browser clock/timezone sane check");
 }
@@ -406,7 +432,9 @@
 layers:{t:"Layers",w:"Each toggle draws one public data stream on the map. Colored dot = its map color. Badge = confidence class. Right number = data age.",s:"—",f:"—",c:"—",l:"Confidence vocabulary: MEASURED (instrument), OFFICIAL (government alert product), MODELED (computed), MOCK (sample, always labeled), EXPERIMENTAL."},
 quakes:{t:"Earthquakes",w:"Every earthquake USGS has published for the selected window. Circle size = magnitude, color = how recent (bright = newer). Click any circle for details.",s:"USGS earthquake.usgs.gov GeoJSON feed",f:"Feed updates ~1 min; small quakes can take minutes–hours to appear",c:"MEASURED",l:"Magnitudes are revised after analyst review; early values shift."},
 alerts:{t:"US Weather Alerts",w:"Active National Weather Service warnings/watches/advisories. Polygons drawn where NWS provides geometry; all point alerts for your location appear in the ALERTS tab and Local Brief.",s:"NWS api.weather.gov (official)",f:"Refreshed every 3 min",c:"OFFICIAL",l:"US only. Some alerts have no polygon (county-based) so they list but don't draw."},
-radar:{t:"Live Radar (NEXRAD)",w:"US composite radar reflectivity served in roughly 5-minute frames. The loop shows recent storm motion, but it is a processed national mosaic rather than raw radar-volume data and should not be described as the fastest available radar product.",s:"NOAA NEXRAD via Iowa Environmental Mesonet tile cache",f:"New frame every ~5 min",c:"MEASURED",l:"US coverage only. Reflectivity ≠ rain at ground (virga, birds, ground clutter possible). Spans beyond 50 min switch to IEM's timestamped ARCHIVE composites (years deep, 5-min steps) — EXPERIMENTAL: a blank frame is an archive gap, not clear skies; the frame label always shows how far back you are looking."},
+radar:{t:"Live Radar (NEXRAD)",w:"US composite radar reflectivity served in roughly 5-minute frames. The timeline now supports scrubbing, stepping, playback speed, crossfades and longer archive spans.",s:"NOAA NEXRAD via Iowa Environmental Mesonet tile cache",f:"New frame every ~5 min",c:"MEASURED",l:"US coverage only. Reflectivity ≠ rain at ground (virga, birds, ground clutter possible). Spans beyond 50 min use IEM timestamped archive composites — EXPERIMENTAL: a blank frame can be an archive gap, not clear skies."},
+lightning:{t:"Recent Lightning",w:"Recent lightning flash/detection locations around the focused point, colored by age. The ring marks the exact search radius. Counts, nearest distance, and a cautious recent-versus-prior activity trend appear in Weather.",s:"Weatherbit Current Lightning API; source field retained per detection. NOAA GLM viewer is linked as the official no-key visual cross-check.",f:"Sensor-derived regions update about every 5 min; Sentinel polls no faster than every 15 min to protect quota",c:"MEASURED / DERIVED",l:"A satellite lightning flash can be in-cloud or cloud-to-ground; do not treat every point as a ground strike. Weatherbit access depends on account plan and each call consumes provider quota."},
+weathercore:{t:"Weather Core",w:"A focused operating mode that brings radar, lightning, NWS alerts, measured station observations and model forecast into one desk. Entering Weather Core temporarily hides unrelated layers; leaving restores your prior layer state.",s:"NWS + NEXRAD/IEM + Weatherbit (optional) + NOAA GLM viewer + Open-Meteo",f:"Per source",c:"Mixed, explicitly labeled",l:"Sentinel is situational awareness, not a substitute for NOAA Weather Radio or local emergency instructions. Radar motion and lightning trends are observations, not deterministic arrival promises."},
 fires:{t:"Active Fires",w:"Satellite thermal-anomaly detections. Click a dot for detection time, satellite, and FIRMS confidence.",s:"NASA FIRMS (VIIRS), free key in ⚙",f:"Satellites revisit every 3–6 h; refreshed 15 min",c:"MEASURED",l:"A detection is a heat anomaly, not a confirmed fire (gas flares, industry trigger it). Clouds hide fires."},
 iss:{t:"ISS Live Position",w:"The International Space Station right now, with its recent ground trail. It orbits every ~92 min at ~7.66 km/s.",s:"WhereTheISS.at (TLE propagation)",f:"Every 65 s",c:"MEASURED",l:"~km-scale accuracy. Pass predictions for your yard: Phase 2 (needs SGP4)."},
 aircraft:{t:"Aircraft (ADS-B)",w:"Aircraft broadcasting ADS-B near the map center. Click for callsign, altitude, speed, heading.",s:"OpenSky Network, anonymous tier",f:"On refresh, ~60 s",c:"MEASURED",l:"HONEST LIMIT: OpenSky heavily restricts anonymous access (a few hundred requests/day, sometimes blocked entirely). If the Watchdog shows this yellow/red, that is OpenSky refusing anonymous traffic — not a bug here. Reliable feed needs their free OAuth account + a tiny proxy (Phase 3). Coverage depends on volunteer receivers; military/some GA aircraft don't broadcast."},
@@ -414,7 +442,7 @@
 spacewx:{t:"Space Weather",w:"Kp (global geomagnetic disturbance 0–9), solar-wind speed measured 1.5M km upstream, and GOES X-ray flux (flare class A/B/C/M/X).",s:"NOAA SWPC + NASA DONKI",f:"1–5 min products",c:"MEASURED",l:"Solar wind at L1 arrives at Earth 15–60 min later. Storm effects on grid/GPS/HF are probabilistic — this app words them as watch conditions only."},
 terminator:{t:"Day/Night Line",w:"Where it is currently daylight vs night, computed from the sun's position.",s:"Computed locally",f:"Continuous",c:"MODELED",l:"±minutes accuracy (equation of time simplified)."},
 power:{t:"Power Infrastructure (OSM)",w:"Power plants, substations, and transmission lines that volunteers have mapped in OpenStreetMap — the same public data behind OpenInfraMap. Zoom into your area (Wise County works well) and press Scan. Click any feature for name, operator, voltage, output where mapped.",s:"OpenStreetMap via Overpass API (ODbL)",f:"On demand",c:"MEASURED (crowd-mapped)",l:"Coverage varies — unmapped ≠ nonexistent. This is lawful public map data only: no private topology, no vulnerability info, no non-public detail, ever."},
-brief:{t:"Local Brief",w:"Everything relevant to your searched location in one card: conditions, nearest quake, nearest fire, active alerts, space weather, moon.",s:"All active adapters",f:"Follows each source",c:"mixed, labeled per line",l:"Only as complete as the sources feeding it."},
+brief:{t:"Local Brief",w:"Everything relevant to your searched location in one card: conditions, lightning, nearest quake, nearest fire, active alerts, space weather, moon.",s:"All active adapters",f:"Follows each source",c:"mixed, labeled per line",l:"Only as complete as the sources feeding it."},
 watchconditions:{t:"Anomalies & Watch Conditions",w:"The rule engine compares layers against each other and against baseline. Every finding shows severity, confidence, sources, why it fired, and what would increase confidence.",s:"Rule engine (local)",f:"Every refresh",c:"MODELED",l:"Hypotheses, not predictions. Nothing here is worded as certainty unless the underlying source is an official alert."},
 moon:{t:"Moon & Sun",w:"Current moon phase, illumination %, and the subsolar point driving the day/night line.",s:"Computed locally",f:"Continuous",c:"MODELED",l:"±minutes; no topocentric correction."},
 counties:{t:"County Lines & Names",w:"US county boundaries with names appearing as you zoom in (names from zoom ~7). Wise County is drawn from the same official geometry as every other county.",s:"US Census Bureau cartographic boundary file, vendored in this repo (public domain)",f:"Static — county lines rarely change",c:"OFFICIAL",l:"US only. Simplified to ~110 m precision for speed — fine for orientation, not for legal property boundaries."},
@@ -422,8 +450,8 @@
 watchdog:{t:"Watchdog",w:"The app monitoring itself: every source's freshness, failures, rate limits, stale caches, and a guard that flags RED if mock data ever appears while in live mode. Green = healthy, yellow = degraded, red = failure, gray = not configured.",s:"Internal checks",f:"Sweeps every 30 s",c:"—",l:"It can only see what the sources return — a provider sending wrong-but-well-formed data is caught only if it contradicts another source."},
 ledger:{t:"Truth Ledger",w:"An append-only log of every fetch this session: source, URL, time, result, confidence class, and known limits. If you wonder where any number came from, the answer is here.",s:"Internal",f:"Written at every fetch",c:"—",l:"Lives in this browser session; Phase 3 adds durable history in a database."},
 sources:{t:"Sources & Integrity",w:"The registry of every data source with its terms posture, refresh rate, and honest limitations, plus this app's standing integrity rules.",s:"Internal",f:"—",c:"—",l:"—"},
-notif:{t:"Notifications",w:"When ON, your browser notifies you even in another tab for: new official NWS alerts at your home location, quakes M4.5+ within ~300 mi of home, Kp reaching 6+ (strong geomagnetic storm), or a fire detection within ~50 mi of home. Each event notifies once (deduplicated).",s:"Rules run locally on the same public feeds",f:"Evaluated every refresh cycle",c:"mixed, per underlying source",l:"Only works while the tab is open (a PWA/service-worker version is Phase 3). Browser must grant permission. This is convenience, not a life-safety system — a NOAA weather radio does not depend on your browser."},
-modes:{t:"Header Modes",w:"AMBIENT = slow, beautiful globe auto-rotation for leaving on a wall screen; touching the map pauses it. INTEGRITY = hides anything mock, experimental, or low-confidence — only verified live data remains. MOCK = clearly-banner-labeled sample data for testing the interface. 🔔 = browser notifications for your home watch zone. ◱ FOCUS = hides every panel so the map alone fills the window. ⛶ = true fullscreen (press Esc to exit). Stack all three — Fullscreen + Focus + Ambient — for a wall display.",s:"—",f:"—",c:"—",l:"MOCK and live can never mix silently: the Watchdog flags RED if mock items appear outside Mock mode."},
+notif:{t:"Notifications",w:"When ON, your browser notifies you even in another tab for: new official NWS alerts independently fetched for home, recent lightning within ~10 mi when the lightning query is focused on home, quakes M4.5+ within ~300 mi, Kp reaching 6+, or a fire detection within ~50 mi. Each event notifies once (deduplicated).",s:"Rules run locally on the same public feeds",f:"Evaluated every refresh cycle",c:"mixed, per underlying source",l:"Only works while the tab is open (a PWA/service-worker version is Phase 3). Browser must grant permission. This is convenience, not a life-safety system — a NOAA weather radio does not depend on your browser."},
+modes:{t:"Header Modes",w:"WX = focused Weather Core; it temporarily shows radar, lightning, alerts and county context, then restores your prior layers when closed. AMBIENT = slow, beautiful globe auto-rotation for leaving on a wall screen; touching the map pauses it. INTEGRITY = hides anything mock, experimental, or low-confidence — only verified live data remains. MOCK = clearly-banner-labeled sample data for testing the interface. 🔔 = browser notifications for your home watch zone. ◱ FOCUS = hides every panel so the map alone fills the window. ⛶ = true fullscreen (press Esc to exit). Stack all three — Fullscreen + Focus + Ambient — for a wall display.",s:"—",f:"—",c:"—",l:"MOCK and live can never mix silently: the Watchdog flags RED if mock items appear outside Mock mode."},
 views:{t:"Views: Globe, Flat World, Flat Polar",w:"Three ways to see the same live data. Globe: the 3D sphere — drag with right-click or two fingers to pitch and orbit it. Flat World: a rectangular world map; with the ISS layer on, its ground track draws the wave pattern seen on NASA mission-control walls. Flat Map (Polar): North Pole at center, Antarctica along the rim — the layout of the UN emblem and the Flat Earth Society map — fully interactive: scroll to zoom, drag to spin, hover for coordinates, with quakes, fires, grid regions, and the live ISS drawn on it.",s:"Identical data across all three views",f:"—",c:"—",l:"The Polar view now takes over the full map area: drag to pan anywhere, scroll to zoom (anchored to your cursor), ⌂ recenters on home, US county lines appear as you zoom, and past a certain zoom a 'Continue to street level' button hands the exact spot to the tile-based views — at neighborhood scale every projection converges, so the hand-off is seamless. Its own renderer draws it because the tile engine only serves Globe and Mercator."},
 perf:{t:"Performance Modes",w:"AUTO senses your device (memory/cores) and picks; LOW renders at 1× scale and drops the densest labels for phones and weak laptops; HIGH is full quality. The genuinely heavy layers, in order: Satellites (SGP4 math for hundreds of objects), Radar Loop (12 tile textures), Fault Lines (13,696 lines), Starlink group. Toggle those off first if anything stutters.",s:"—",f:"—",c:"—",l:"AUTO's device sensing is approximate — browsers deliberately fuzz hardware info."},
 customkeys:{t:"Your Own Keys",w:"A place to store ANY API key you find, with your own label. Saved locally, included in key export and FULL backup. When a future adapter needs that provider, the key is already waiting — no rebuild needed.",s:"You",f:"—",c:"—",l:"Storing a key does not create an adapter — data starts flowing when an adapter for that provider exists. Ask and it gets built into the next version."},
@@ -618,6 +646,9 @@
     properties:{mag:q.mag,place:q.place,ageH:(now()-q.t)/36e5,depth:q.depth,mock:!!q.mock,t:q.t}}))}),
   fires:()=>({type:"FeatureCollection",features:S.fires.map(f=>({type:"Feature",
     geometry:{type:"Point",coordinates:[f.lon,f.lat]},properties:{conf:f.conf,when:f.when,sat:f.sat,mock:!!f.mock}}))}),
+  lightning:()=>({type:"FeatureCollection",features:S.lightning.map(x=>({type:"Feature",
+    geometry:{type:"Point",coordinates:[x.lon,x.lat]},properties:{id:String(x.id||""),pastMins:x.pastMins,distanceKm:x.distanceKm,source:x.source,type:x.type,t:x.t,mock:!!x.mock}}))}),
+  lightningzone:()=>S.loc?radiusPolygon(S.loc.lat,S.loc.lon,cfg.lightningRadius):EMPTY,
   aircraft:()=>({type:"FeatureCollection",features:S.aircraft.map(a=>({type:"Feature",
     geometry:{type:"Point",coordinates:[a.lon,a.lat]},properties:{cs:a.cs,alt:a.alt,vel:a.vel,hdg:a.hdg,icao:a.icao}}))}),
   iss:()=>S.iss?{type:"FeatureCollection",features:[{type:"Feature",
@@ -693,6 +724,12 @@
       "icon-size":["interpolate",["linear"],["coalesce",["get","mag"],3],2,0.35,4.5,0.65,6,0.95,7.5,1.3]}});
   map.addLayer({id:"fires",type:"symbol",source:"fires",
     layout:{"icon-image":"i-fire","icon-size":0.5,"icon-allow-overlap":true}});
+  map.addLayer({id:"lightning-zone-fill",type:"fill",source:"lightningzone",layout:{visibility:"none"},paint:{"fill-color":"#58c7e8","fill-opacity":0.035}});
+  map.addLayer({id:"lightning-zone",type:"line",source:"lightningzone",layout:{visibility:"none"},paint:{"line-color":"#58c7e8","line-width":1.2,"line-opacity":0.65,"line-dasharray":[3,2]}});
+  map.addLayer({id:"lightning",type:"circle",source:"lightning",layout:{visibility:"none"},paint:{
+    "circle-radius":["interpolate",["linear"],["zoom"],3,3,9,6,14,9],
+    "circle-color":["case",["<=",["get","pastMins"],5],"#ff496c",["<=",["get","pastMins"],15],"#ffb84d","#ffe56b"],
+    "circle-opacity":0.88,"circle-stroke-color":"#fff7c2","circle-stroke-width":1,"circle-stroke-opacity":0.75}});
   map.addLayer({id:"aircraft",type:"symbol",source:"aircraft",
     layout:{"icon-image":"i-plane","icon-size":0.5,"icon-allow-overlap":true,
       "icon-rotate":["-",["coalesce",["get","hdg"],0],45],"icon-rotation-alignment":"map"}});
@@ -710,6 +747,8 @@
     <br><span style="color:#8fa69e">USGS · MEASURED${p.mock==="true"||p.mock===true?" · <b>MOCK</b>":""}</span>`,e.lngLat);});
   map.on("click","fires",e=>{const p=e.features[0].properties;
     pop(`<b>Thermal detection</b><br>${esc(p.when)} UTC · sat ${esc(p.sat)}<br>FIRMS confidence: ${esc(p.conf)}<br><span style="color:#8fa69e">NASA FIRMS · detection ≠ confirmed fire${p.mock==="true"||p.mock===true?" · <b>MOCK</b>":""}</span>`,e.lngLat);});
+  map.on("click","lightning",e=>{const p=e.features[0].properties;
+    pop(`<b>⚡ Recent lightning ${esc(p.type||"detection")}</b><br>${(+p.pastMins).toFixed(1)} min ago · ${(+p.distanceKm*0.621371).toFixed(1)} mi from focus<br>source ${esc(p.source||"provider")}${p.mock==="true"||p.mock===true?" · <b>MOCK</b>":""}<br><span style="color:#8fa69e">MEASURED location/time. Satellite flashes can be in-cloud; point does not prove a ground strike.</span>`,e.lngLat);});
   map.on("click","aircraft",async e=>{const p=e.features[0].properties;
     const pid="acr"+now();
     pop(`<b>${esc(p.cs||p.icao)}</b><br>alt ${p.alt?Math.round(p.alt)+" m":"—"} · ${p.vel?Math.round(p.vel*1.944)+" kt":"—"} · hdg ${p.hdg?Math.round(p.hdg)+"°":"—"}
@@ -751,7 +790,7 @@
     pop(`<b>Substation</b> ${esc(p.name||"")}<br>${p.volt?"Voltage: "+esc(p.volt)+"<br>":""}<span style="color:#8fa69e">OpenStreetMap (ODbL)</span>`,e.lngLat);});
   map.on("click","power-lines",e=>{const p=e.features[0].properties;
     pop(`<b>Transmission line</b><br>${p.volt?"Voltage: "+esc(p.volt)+"<br>":""}<span style="color:#8fa69e">OpenStreetMap (ODbL)</span>`,e.lngLat);});
-  ["quakes","fires","aircraft","ba","power-plants","power-subs","power-lines"].forEach(l=>{
+  ["quakes","fires","lightning","aircraft","ba","power-plants","power-subs","power-lines"].forEach(l=>{
     map.on("mouseenter",l,()=>map.getCanvas().style.cursor="pointer");
     map.on("mouseleave",l,()=>map.getCanvas().style.cursor="");});
   wd("map","ok","MapLibre v5 globe ready");
@@ -759,67 +798,62 @@
 map.on("error",e=>{const m=e&&e.error&&e.error.message||"map error";
   if(!/tile/i.test(m))wd("map","warn",m);});
 
-/* ---------- radar loop ---------- */
-let radarPlaying=false, radarFrame=RADAR_FRAMES.length-1, radarTimer=null;
+/* ---------- radar loop: scrubber + step controls + crossfade + adaptive hold ---------- */
+let radarPlaying=false, radarFrame=RADAR_FRAMES.length-1, radarTimer=null, radarDelay=650;
 const radarCount=()=>radarSpanMin>50?radarArchive.length:RADAR_FRAMES.length;
-let radarFadeTimer=null,radarPrev=-1;
-function radarOpacityTarget(){const s=document.getElementById("radarOpacity");return s?+s.value:0.72;}
+let radarFadeTimer=null,radarPrev=-1,radarPrefix="radar";
+function radarOpacityTarget(){const e=$("radarOpacity");return e?+e.value:0.72;}
+function radarUpdateControls(){const n=Math.max(1,radarCount()),sc=$("radarScrub");
+  if(sc){sc.max=String(n-1);sc.value=String(Math.max(0,Math.min(n-1,radarFrame)));}
+  $("btnRadarPause").textContent=radarPlaying?"⏸":"▶";
+  $("btnRadarPlay").textContent=radarPlaying?"⏸ Pause radar":"▶ Open fluid radar timeline";}
 function radarCrossfade(prefix,fromI,toI,count){
-  clearInterval(radarFadeTimer);
-  const op=radarOpacityTarget();let step=0,STEPS=6;
+  clearInterval(radarFadeTimer);const op=radarOpacityTarget();let step=0,STEPS=7;
   for(let j=0;j<count;j++){const id=prefix+j;if(!map.getLayer(id))continue;
-    map.setLayoutProperty(id,"visibility",(j===toI||j===fromI)?"visible":"none");
-    if(j!==toI&&j!==fromI)map.setPaintProperty(id,"raster-opacity",0);}
+    const keep=j===toI||(prefix===radarPrefix&&j===fromI);map.setLayoutProperty(id,"visibility",keep?"visible":"none");
+    if(!keep)map.setPaintProperty(id,"raster-opacity",0);}
   radarFadeTimer=setInterval(()=>{step++;const t=step/STEPS;
-    try{
-      if(map.getLayer(prefix+toI))map.setPaintProperty(prefix+toI,"raster-opacity",op*t);
-      if(fromI>=0&&fromI!==toI&&map.getLayer(prefix+fromI))map.setPaintProperty(prefix+fromI,"raster-opacity",op*(1-t));
-    }catch(e){}
-    if(step>=STEPS){clearInterval(radarFadeTimer);
-      if(fromI>=0&&fromI!==toI&&map.getLayer(prefix+fromI))try{map.setLayoutProperty(prefix+fromI,"visibility","none");}catch(e){}}},55);}
-function radarShowLive(i){
-  radarCrossfade("radar",radarPrev,i,RADAR_FRAMES.length);radarPrev=i;
-  const back=(RADAR_FRAMES.length-1-i)*5;
-  $("radarTime").textContent=back===0?"latest":`now − ${back} min`;}
-/* Archive radar: IEM keeps timestamped US composites (ridge::USCOMP-N0Q-YYYYMMDDHHMM, 5-min steps, years deep).
-   EXPERIMENTAL: occasional missing frames are the archive's gaps, not the app's. */
+    try{if(map.getLayer(prefix+toI))map.setPaintProperty(prefix+toI,"raster-opacity",op*t);
+      if(fromI>=0&&fromI!==toI&&prefix===radarPrefix&&map.getLayer(prefix+fromI))map.setPaintProperty(prefix+fromI,"raster-opacity",op*(1-t));}catch(e){}
+    if(step>=STEPS){clearInterval(radarFadeTimer);if(fromI>=0&&fromI!==toI&&prefix===radarPrefix&&map.getLayer(prefix+fromI))try{map.setLayoutProperty(prefix+fromI,"visibility","none");}catch(e){}}},45);}
+function radarPrime(prefix,i,count){if(count<2)return;const n=(i+1)%count,id=prefix+n;
+  if(!map.getLayer(id))return;try{map.setLayoutProperty(id,"visibility","visible");map.setPaintProperty(id,"raster-opacity",0);
+    setTimeout(()=>{if(radarFrame!==n&&map.getLayer(id))map.setLayoutProperty(id,"visibility","none");},1800);}catch(e){}}
+function radarShow(i){const count=radarCount();if(!count)return;radarFrame=Math.max(0,Math.min(count-1,i));
+  const prefix=radarSpanMin>50?"radarA":"radar";
+  if(prefix!==radarPrefix){radarPrev=-1;radarPrefix=prefix;}
+  const other=prefix==="radar"?"radarA":"radar";
+  const otherCount=prefix==="radar"?radarArchive.length:RADAR_FRAMES.length;
+  for(let j=0;j<otherCount;j++)try{map.getLayer(other+j)&&map.setLayoutProperty(other+j,"visibility","none");}catch(e){}
+  if(LAYERS.radar.on)radarCrossfade(prefix,radarPrev,radarFrame,count);radarPrev=radarFrame;
+  if(prefix==="radar"){
+    const back=(RADAR_FRAMES.length-1-radarFrame)*5;$("radarTime").textContent=back===0?"latest":`now − ${back} min`;$("radarLbl").textContent="";
+  }else{const f=radarArchive[radarFrame];$("radarTime").textContent=f?f.label:"archive";$("radarLbl").textContent="EXPERIMENTAL archive";}
+  radarUpdateControls();radarPrime(prefix,radarFrame,count);}
+/* Archive radar: IEM timestamped US composites. Missing frames can be archive gaps. */
 function radarSetSpan(min){
-  radarSpanMin=min;
-  // clear old archive layers
-  radarArchive.forEach((_,j)=>{try{map.getLayer("radarA"+j)&&map.removeLayer("radarA"+j);map.getSource("radarA"+j)&&map.removeSource("radarA"+j);}catch(e){}});
-  radarArchive=[];
-  if(min>50){
-    const N=12,step=min/N;
-    for(let j=0;j<N;j++){
-      const t=new Date(now()-(min-j*step)*6e4);
-      t.setUTCMinutes(Math.floor(t.getUTCMinutes()/5)*5,0,0);
-      const ts=t.toISOString().replace(/[-:TZ]/g,"").slice(0,12);
-      radarArchive.push({ts,label:(min-j*step)>=60?((min-j*step)/60).toFixed(1)+" h ago":(min-j*step)+" min ago"});
-      map.addSource("radarA"+j,{type:"raster",tileSize:256,
-        tiles:[`https://mesonet.agron.iastate.edu/cache/tile.py/1.0.0/ridge::USCOMP-N0Q-${ts}/{z}/{x}/{y}.png`]});
-      map.addLayer({id:"radarA"+j,type:"raster",source:"radarA"+j,
-        layout:{visibility:"none"},paint:{"raster-opacity":0.72}},"county-lines");}
-    ledger("IEM NEXRAD archive","mesonet.agron.iastate.edu",`radar span ${min>=60?min/60+" h":min+" min"} — 12 archived frames (EXPERIMENTAL)`,"OK","MEASURED","archive frames can have gaps; missing imagery = archive gap, not new weather");
-  }
-  radarFrame=(min>50?radarArchive.length:RADAR_FRAMES.length)-1;radarShow(radarFrame);}
-function radarShow(i){
-  if(radarSpanMin>50){
-    RADAR_FRAMES.forEach((_,j)=>{try{map.getLayer("radar"+j)&&map.setLayoutProperty("radar"+j,"visibility","none");}catch(e){}});
-    radarArchive.forEach((f,j)=>{try{map.getLayer("radarA"+j)&&map.setLayoutProperty("radarA"+j,"visibility",j===i&&LAYERS.radar.on?"visible":"none");}catch(e){}});
-    $("radarLbl")&&($("radarLbl").textContent=radarArchive[i]?radarArchive[i].label+" (archive)":"");
-    return;}
-  $("radarLbl")&&($("radarLbl").textContent="");radarShowLive(i);}
-document.addEventListener("change",e=>{if(e.target&&e.target.id==="radarSpan")radarSetSpan(+e.target.value);});
+  radarSpanMin=min;radarPlay(false);
+  radarArchive.forEach((_,j)=>{try{map.getLayer("radarA"+j)&&map.removeLayer("radarA"+j);map.getSource("radarA"+j)&&map.removeSource("radarA"+j);}catch(e){}});radarArchive=[];
+  if(min>50){const N=12,step=min/N;
+    for(let j=0;j<N;j++){const ago=min-j*step,t=new Date(now()-ago*6e4);t.setUTCMinutes(Math.floor(t.getUTCMinutes()/5)*5,0,0);
+      const ts=t.toISOString().replace(/[-:TZ]/g,"").slice(0,12);radarArchive.push({ts,label:ago>=60?(ago/60).toFixed(1)+" h ago":Math.round(ago)+" min ago"});
+      map.addSource("radarA"+j,{type:"raster",tileSize:256,tiles:[`https://mesonet.agron.iastate.edu/cache/tile.py/1.0.0/ridge::USCOMP-N0Q-${ts}/{z}/{x}/{y}.png`]});
+      map.addLayer({id:"radarA"+j,type:"raster",source:"radarA"+j,layout:{visibility:"none"},paint:{"raster-opacity":0}},"county-lines");}
+    ledger("IEM NEXRAD archive","mesonet.agron.iastate.edu",`radar span ${min>=60?min/60+" h":min+" min"} — 12 frames`,"OK","MEASURED","EXPERIMENTAL archive; gaps can render blank");}
+  radarPrev=-1;radarPrefix=min>50?"radarA":"radar";radarFrame=radarCount()-1;radarShow(radarFrame);}
+function radarSchedule(){clearTimeout(radarTimer);if(!radarPlaying)return;
+  const hold=radarFrame===radarCount()-1?Math.round(radarDelay*1.8):radarDelay;
+  radarTimer=setTimeout(()=>{radarFrame=(radarFrame+1)%radarCount();radarShow(radarFrame);radarSchedule();},hold);}
+function radarPlay(on){radarPlaying=!!on;$("radarBar").classList.toggle("show",radarPlaying||LAYERS.radar.on);radarUpdateControls();
+  if(radarPlaying)radarSchedule();else clearTimeout(radarTimer);}
+function radarStep(d){radarPlay(false);radarShow((radarFrame+d+radarCount())%radarCount());}
+document.addEventListener("change",e=>{if(e.target&&e.target.id==="radarSpan")radarSetSpan(+e.target.value);
+  if(e.target&&e.target.id==="radarSpeed"){radarDelay=+e.target.value||650;if(radarPlaying)radarSchedule();}});
 document.addEventListener("input",e=>{if(e.target&&e.target.id==="radarOpacity"){const v=+e.target.value;
-  RADAR_FRAMES.forEach((_,j)=>{try{map.getLayer("radar"+j)&&map.setPaintProperty("radar"+j,"raster-opacity",v);}catch(x){}});
-  radarArchive.forEach((_,j)=>{try{map.getLayer("radarA"+j)&&map.setPaintProperty("radarA"+j,"raster-opacity",v);}catch(x){}});}});
-function radarPlay(on){
-  radarPlaying=on;$("radarBar").classList.toggle("show",on||LAYERS.radar.on);
-  clearInterval(radarTimer);
-  if(on){radarTimer=setInterval(()=>{radarFrame=(radarFrame+1)%radarCount();
-    radarShow(radarFrame);},radarFrame===radarCount()-1?1400:450);
-    $("btnRadarPause").textContent="⏸";}
-  else{radarFrame=radarCount()-1;if(LAYERS.radar.on)radarShow(radarFrame);$("btnRadarPause").textContent="▶";}}
+    RADAR_FRAMES.forEach((_,j)=>{try{map.getLayer("radar"+j)&&map.setPaintProperty("radar"+j,"raster-opacity",j===radarFrame?v:0);}catch(x){}});
+    radarArchive.forEach((_,j)=>{try{map.getLayer("radarA"+j)&&map.setPaintProperty("radarA"+j,"raster-opacity",j===radarFrame?v:0);}catch(x){}});}
+  if(e.target&&e.target.id==="radarScrub"){radarPlay(false);radarShow(+e.target.value);}});
+$("radarBack").addEventListener("click",()=>radarStep(-1));$("radarNext").addEventListener("click",()=>radarStep(1));
 
 /* ---------- ambient rotation: the always-on beautiful globe ---------- */
 let ambientRAF=null,lastInteract=0,userBusy=false;
@@ -875,6 +909,7 @@
    ===================================================================== */
 const LAYERS={
   radar:{name:"Live Radar (NEXRAD, US)",color:"#3fa9f5",on:true,badge:"MEASURED",info:"radar",maps:["radar"+(RADAR_FRAMES.length-1)]},
+  lightning:{name:"Recent Lightning (keyed)",color:"#ffcf4a",on:true,badge:"MEASURED",info:"lightning",maps:["lightning","lightning-zone","lightning-zone-fill"]},
   alerts:{name:"Weather Alerts (US)",color:"#d96a5b",on:true,badge:"OFFICIAL",info:"alerts",maps:["alertpolys","alertlines"]},
   quakes:{name:"Earthquakes",color:"#e0b24b",on:true,badge:"MEASURED",info:"quakes",maps:["quakes"]},
   fires:{name:"Active Fires (FIRMS)",color:"#ff7a3c",on:true,badge:"MEASURED",info:"fires",maps:["fires"]},
@@ -911,6 +946,7 @@
   const L=LAYERS[s.dataset.layer];L.on=!L.on;applyLayerVisibility();renderLayerList();
   if(s.dataset.layer==="traffic")ensureTraffic();
   if(s.dataset.layer==="ships")shipsConnect();
+  if(s.dataset.layer==="lightning"&&L.on)runAdapter("lightning",true);
   if(s.dataset.layer==="flow"&&L.on)runAll(true);});
 
 /* ---- Road traffic: TomTom flow tiles (free key). Current speeds only — TomTom keeps no public history. ---- */
@@ -1201,28 +1237,32 @@
   ledger("USGS","earthquake.usgs.gov",S.quakes.length+" quakes (24h)","OK","MEASURED","early magnitudes get revised");}},
 alerts:{name:"NWS Alerts",int:180,staleSec:900,enabled:()=>true,
  run:async()=>{
-  const rp=await fetch(`https://api.weather.gov/alerts/active?point=${S.loc.lat.toFixed(3)},${S.loc.lon.toFixed(3)}`,
-    {headers:{"Accept":"application/geo+json"}});
-  if(!rp.ok)throw new Error("HTTP "+rp.status);
-  const jp=await rp.json();
-  const local=(jp.features||[]).map(f=>({event:f.properties.event,sev:f.properties.severity,
-    headline:f.properties.headline,area:f.properties.areaDesc,ends:f.properties.ends,
-    desc:f.properties.description,sender:f.properties.senderName,geom:f.geometry,local:true}));
-  let national=[];
-  try{const rn=await fetch("https://api.weather.gov/alerts/active?severity=Extreme,Severe&limit=180",
-    {headers:{"Accept":"application/geo+json"}});
-    if(rn.ok){const jn=await rn.json();
-      national=(jn.features||[]).filter(f=>f.geometry).map(f=>({event:f.properties.event,sev:f.properties.severity,
-        headline:f.properties.headline,area:f.properties.areaDesc,geom:f.geometry}));}}catch(e){}
-  const seen=new Set(local.map(a=>a.headline));
-  S.alerts=[...local,...national.filter(a=>!seen.has(a.headline))];
-  gj("alertpolys");FRESH.alerts=now();
-  ledger("NWS","api.weather.gov",`${local.length} local + ${national.length} national severe alerts`,"OK","OFFICIAL","county alerts may lack polygons");}},
+  const fetchPoint=async(lat,lon,flags)=>{const rp=await fetch(`https://api.weather.gov/alerts/active?point=${lat.toFixed(3)},${lon.toFixed(3)}`,{headers:{"Accept":"application/geo+json"}});
+    if(!rp.ok)throw new Error("point alerts HTTP "+rp.status);const jp=await rp.json();return (jp.features||[]).map(f=>({event:f.properties.event,sev:f.properties.severity,
+      headline:f.properties.headline,area:f.properties.areaDesc,ends:f.properties.ends,desc:f.properties.description,sender:f.properties.senderName,geom:f.geometry,...flags}));};
+  const atHome=haversine(S.loc.lat,S.loc.lon,cfg.home.lat,cfg.home.lon)<1;
+  const local=await fetchPoint(S.loc.lat,S.loc.lon,{local:true,home:atHome});
+  const home=atHome?[]:await fetchPoint(cfg.home.lat,cfg.home.lon,{home:true});
+  let national=[];try{const rn=await fetch("https://api.weather.gov/alerts/active?severity=Extreme,Severe&limit=180",{headers:{"Accept":"application/geo+json"}});
+    if(rn.ok){const jn=await rn.json();national=(jn.features||[]).filter(f=>f.geometry).map(f=>({event:f.properties.event,sev:f.properties.severity,headline:f.properties.headline,area:f.properties.areaDesc,geom:f.geometry}));}}catch(e){}
+  const by=new Map();for(const a of [...local,...home,...national]){const k=(a.headline||a.event||"")+"|"+(a.area||"");const old=by.get(k);if(old){old.local=old.local||a.local;old.home=old.home||a.home;if(!old.geom&&a.geom)old.geom=a.geom;}else by.set(k,a);}S.alerts=[...by.values()];
+  gj("alertpolys");FRESH.alerts=now();ledger("NWS","api.weather.gov",`${local.length} focused + ${home.length||(atHome?local.length:0)} home + ${national.length} national severe alerts`,"OK","OFFICIAL","home watch alerts are fetched independently from the viewed location; county alerts may lack polygons");}},
 meteo:{name:"Open-Meteo Conditions",int:600,staleSec:2400,enabled:()=>true,
- run:async()=>{const r=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${S.loc.lat}&longitude=${S.loc.lon}&current=temperature_2m,apparent_temperature,relative_humidity_2m,precipitation,weather_code,wind_speed_10m,wind_gusts_10m&hourly=temperature_2m,precipitation_probability&daily=temperature_2m_max,temperature_2m_min,precipitation_probability_max,sunrise,sunset&forecast_days=7&timezone=auto&temperature_unit=fahrenheit&wind_speed_unit=mph`);
+ run:async()=>{const r=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${S.loc.lat}&longitude=${S.loc.lon}&current=temperature_2m,apparent_temperature,relative_humidity_2m,precipitation,weather_code,wind_speed_10m,wind_gusts_10m&hourly=temperature_2m,precipitation_probability,precipitation,weather_code,wind_speed_10m,wind_gusts_10m&daily=weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,sunrise,sunset&forecast_days=7&timezone=auto&temperature_unit=fahrenheit&wind_speed_unit=mph&precipitation_unit=inch`);
   if(!r.ok)throw new Error("HTTP "+r.status);const j=await r.json();S.meteo=j.current;
   S.wx={hourly:j.hourly,daily:j.daily};FRESH.meteo=now();
   ledger("Open-Meteo","api.open-meteo.com","model nowcast + hourly + 7-day forecast","OK","MODELED","current fields are model-derived nowcast values; station observations are fetched separately from NWS when available");}},
+lightning:{name:"Weatherbit Recent Lightning",int:900,staleSec:1800,enabled:()=>!!cfg.weatherbitKey&&LAYERS.lightning.on,offNote:"needs Weatherbit key/eligible plan + Lightning layer on",
+ run:async()=>{const radius=Math.round(Math.max(10,Math.min(75,cfg.lightningRadius))),mins=Math.round(Math.max(10,Math.min(45,cfg.lightningMinutes)));
+  const u=`https://api.weatherbit.io/v2.0/current/lightning?lat=${S.loc.lat.toFixed(4)}&lon=${S.loc.lon.toFixed(4)}&search_distance_km=${radius}&search_mins=${mins}&limit=200&sort=time&key=${encodeURIComponent(cfg.weatherbitKey)}`;
+  const r=await fetch(u);if(r.status===204){S.lightning=[];S.lightningMeta={count:0,returned:0,radius,mins,t:now()};gj("lightning");gj("lightningzone");FRESH.lightning=now();ledger("Weatherbit Lightning","api.weatherbit.io",`no lightning returned within ${radius} km / ${mins} min`,"OK","MEASURED","quiet result; provider quota still applies");return;}
+  if(!r.ok){let msg="HTTP "+r.status;try{const e=await r.json();if(e.error)msg+=" · "+e.error;}catch(e){}throw new Error(msg);}
+  const j=await r.json(),arr=Array.isArray(j.lightning)?j.lightning:[];
+  S.lightning=arr.map(x=>{let t=Date.parse(x.timestamp_utc||"");if(!isFinite(t)&&x.timestamp_utc)t=Date.parse(x.timestamp_utc+"Z");
+    const past=Number.isFinite(+x.past_mins)?+x.past_mins:(isFinite(t)?(now()-t)/6e4:0);return{id:x.id,lat:+x.lat,lon:+x.lon,pastMins:past,distanceKm:Number.isFinite(+x.distance_km)?+x.distance_km:haversine(S.loc.lat,S.loc.lon,+x.lat,+x.lon),source:x.source||"unknown",type:x.type||"detection",t:isFinite(t)?t:now()-past*6e4};
+  }).filter(x=>isFinite(x.lat)&&isFinite(x.lon)&&x.lat>=-90&&x.lat<=90&&x.lon>=-180&&x.lon<=180);
+  S.lightningMeta={count:Number.isFinite(+j.count)?+j.count:S.lightning.length,returned:S.lightning.length,radius,mins,t:now()};gj("lightning");gj("lightningzone");FRESH.lightning=now();
+  ledger("Weatherbit Lightning","api.weatherbit.io",`${S.lightningMeta.count} lightning observations within ${radius} km / ${mins} min (${S.lightning.length} mapped)`,"OK","MEASURED","source may be satellite GLM or radar estimate; satellite flash does not prove ground strike; provider quota applies");}},
 nwsobs:{name:"NWS Station Observation",int:900,staleSec:5400,enabled:()=>S.loc.lat>24&&S.loc.lat<50&&S.loc.lon>-125&&S.loc.lon<-66,offNote:"US locations only",
  run:async()=>{
   const r=await fetch(`https://api.weather.gov/points/${S.loc.lat.toFixed(4)},${S.loc.lon.toFixed(4)}`);
@@ -1366,6 +1406,7 @@
  alerts:{lbl:"Alerts",url:"https://www.weather.gov/",alt:"US-official; no equal substitute. If down, check weather.gov directly or NOAA Weather Radio."},
  nwsobs:{lbl:"StationWx",url:"https://www.weather.gov/",alt:"Nearest NWS/ASOS station — a real thermometer. If it disagrees with the model row, trust the station."},
  meteo:{lbl:"Weather",url:"https://open-meteo.com/",alt:"Model-based; NWS point forecast (forecast.weather.gov) is the official alternative."},
+ lightning:{lbl:"Lightning",url:"https://www.star.nesdis.noaa.gov/goes/conus_band.php?band=EXTENT3&length=24&sat=G19",alt:"Weatherbit point feed is keyed/plan-dependent. NOAA GLM is the official no-key visual cross-check and includes total lightning, not only ground strikes."},
  kp:{lbl:"Kp",url:"https://www.swpc.noaa.gov/products/planetary-k-index",alt:"NOAA SWPC is the operational source; GFZ Potsdam (kp.gfz-potsdam.de) computes the definitive Kp."},
  wind:{lbl:"SolarWind",url:"https://www.swpc.noaa.gov/products/real-time-solar-wind",alt:"Same DSCOVR data is on the SWPC dashboard directly."},
  xray:{lbl:"X-ray",url:"https://www.swpc.noaa.gov/products/goes-x-ray-flux",alt:"GOES X-ray plots on SWPC directly."},
@@ -1421,13 +1462,16 @@
     S.quakes=[{mag:4.6,place:"12 km NW of Sample, OK (MOCK)",t:t-2*36e5,lon:-97.9,lat:34.4,depth:6,mock:true},
               {mag:2.9,place:"Sample cluster (MOCK)",t:t-9e5,lon:-97.7,lat:33.5,depth:4,mock:true}];
     S.fires=[{lat:33.5,lon:-98.1,conf:"h",when:"(MOCK)",sat:"MOCK",mock:true}];
+    S.lightning=[{id:"mock1",lat:33.36,lon:-97.72,pastMins:2.5,distanceKm:18,source:"MOCK",type:"flash",t:t-150000,mock:true},
+      {id:"mock2",lat:33.12,lon:-97.42,pastMins:11,distanceKm:29,source:"MOCK",type:"flash",t:t-660000,mock:true}];
+    S.lightningMeta={count:2,returned:2,radius:cfg.lightningRadius,mins:cfg.lightningMinutes,t};
     S.alerts=[{event:"Heat Advisory (MOCK)",sev:"Moderate",headline:"SAMPLE — labeled mock",area:"Wise County, TX (SAMPLE)",
       sender:"MOCK",mock:true,geom:{type:"Polygon",coordinates:[[[-98.2,33.0],[-97.2,33.0],[-97.2,33.7],[-98.2,33.7],[-98.2,33.0]]]}}];
     S.kp={v:5.3,t:now()};S.wind={speed:612,dens:4.2,t:now()};S.xray={flux:2.3e-5,t:now()};S.imf={bz:-8.4,bt:12.1,t:now()};
     S.ba={ERCO:{code:"ERCO",name:"ERCOT (MOCK)",lat:31.2,lon:-99.3,d:71000,df:66000,period:"MOCK"}};
     ledger("MOCK MODE","","sample data injected — ALL items tagged MOCK","OK","MOCK","demo only");
-  }else{S.quakes=[];S.fires=[];S.alerts=[];S.ba={};ledger("MOCK MODE","","cleared — resuming live fetches","OK","OFFICIAL","");}
-  ["quakes","fires","alertpolys","ba"].forEach(gj);
+  }else{S.quakes=[];S.fires=[];S.lightning=[];S.lightningMeta=null;S.alerts=[];S.ba={};ledger("MOCK MODE","","cleared — resuming live fetches","OK","OFFICIAL","");}
+  ["quakes","fires","lightning","lightningzone","alertpolys","ba"].forEach(gj);
   runAnomalyEngine();renderAll();
   if(!on)runAll(true);}
 
@@ -1450,6 +1494,11 @@
     "GOES X-ray flux at M/X level. Possible shortwave radio fade on the daylit side of Earth during the flare.",
     "NOAA SWPC/GOES","Flare location on the Earth-facing disk (DONKI) determines whether follow-on effects matter.");
   if(loc){
+    const closeL=S.lightning.filter(x=>x.pastMins<=10&&haversine(loc.lat,loc.lon,x.lat,x.lon)<=16.1).sort((a,b)=>a.distanceKm-b.distanceKm);
+    if(closeL.length){const x=closeL[0],mi=haversine(loc.lat,loc.lon,x.lat,x.lon)*0.621371;
+      push(mi<=5?3:2,"HIGH",`Recent lightning ${mi.toFixed(1)} mi from ${loc.name.split(",")[0]}`,
+        `${closeL.length} returned flash/detection(s) within 10 mi during the last 10 minutes. This is immediate proximity evidence, not a claim that every flash reached the ground.`,
+        `Weatherbit / ${x.source||"provider"}`,"Continued detections moving closer, or an official NWS warning naming the storm, would increase concern.",{lon:x.lon,lat:x.lat,z:10});}
     const nearQ=S.quakes.filter(q=>haversine(loc.lat,loc.lon,q.lat,q.lon)<300);
     const big=nearQ.filter(q=>q.mag>=4);
     if(big.length)push(2,"HIGH",`M${big[0].mag.toFixed(1)} earthquake within 300 mi of ${loc.name.split(",")[0]}`,
@@ -1554,6 +1603,7 @@
   const l=S.loc;if(!l){$("brief").textContent="Search a location.";return;}
   const m=moonInfo();const nearestQ=S.quakes.map(q=>({q,d:haversine(l.lat,l.lon,q.lat,q.lon)})).sort((a,b)=>a.d-b.d)[0];
   const nearestF=S.fires.map(f=>({f,d:haversine(l.lat,l.lon,f.lat,f.lon)})).sort((a,b)=>a.d-b.d)[0];
+  const nearestL=S.lightning.map(x=>({x,d:haversine(l.lat,l.lon,x.lat,x.lon)})).sort((a,b)=>a.d-b.d)[0];
   const locAl=S.alerts.filter(a=>a.local);
   $("brief").innerHTML=`<div style="font-size:13px;color:var(--vir3);margin-bottom:5px">${esc(l.name)}</div>
   ${S.meteo?`<div class="brief-metric"><span>Temp / humidity</span><span class="v">${Math.round(S.meteo.temperature_2m)}°F · ${S.meteo.relative_humidity_2m}%</span></div>
@@ -1561,6 +1611,7 @@
   <div class="brief-metric"><span>Feels like</span><span class="v">${S.meteo.apparent_temperature!=null?Math.round(S.meteo.apparent_temperature)+"°F":"—"}</span></div>
   <div class="brief-metric"><span>Wind / gusts</span><span class="v">${Math.round(S.meteo.wind_speed_10m)} / ${Math.round(S.meteo.wind_gusts_10m)} mph</span></div>`:""}
   ${wxExtras()}
+  <div class="brief-metric"><span>Recent lightning</span><span class="v">${cfg.weatherbitKey?(S.lightningMeta?S.lightningMeta.count+" · nearest "+(nearestL?(nearestL.d*0.621371).toFixed(1)+" mi":"none"):"loading") : "key optional"}</span></div>
   <div class="brief-metric"><span>Active NWS alerts here</span><span class="v">${locAl.length}</span></div>
   <div class="brief-metric"><span>Nearest quake (24h)</span><span class="v">${nearestQ?`M${nearestQ.q.mag.toFixed(1)} · ${Math.round(nearestQ.d)} km`:"none"}</span></div>
   <div class="brief-metric"><span>Nearest fire detection</span><span class="v">${nearestF?Math.round(nearestF.d)+" km":cfg.firmsKey?"none":"needs key"}</span></div>
@@ -1680,6 +1731,39 @@
    Trust tiers: 1 official operational · 2 official dataset/science · 3 aggregator
    · 4 open/crowdsourced · 5 secondary mirror · 6 model/inference · 7 user journal
    ===================================================================== */
+
+function lightningStats(){const L=S.lightning.slice().sort((a,b)=>a.pastMins-b.pastMins),nearest=L.slice().sort((a,b)=>a.distanceKm-b.distanceKm)[0];
+  const recentL=L.filter(x=>x.pastMins<=10),priorL=L.filter(x=>x.pastMins>10&&x.pastMins<=20),recent=recentL.length,prior=priorL.length;
+  let activity="insufficient sample";if(recent+prior>=4)activity=recent>=prior*1.6?"activity increasing":prior>=recent*1.6?"activity decreasing":"activity roughly steady";
+  const median=a=>{if(!a.length)return null;const b=a.slice().sort((x,y)=>x-y);return b[Math.floor(b.length/2)];},rd=median(recentL.map(x=>x.distanceKm)),pd=median(priorL.map(x=>x.distanceKm));
+  let range="distance trend unavailable";if(rd!=null&&pd!=null&&recent>=2&&prior>=2)range=rd<pd-5?"detections trending closer":rd>pd+5?"detections trending farther":"distance roughly steady";
+  return{L,nearest,recent,prior,activity,range,total:S.lightningMeta?S.lightningMeta.count:L.length};}
+function wxCodeLabel(c){return({0:"Clear",1:"Mainly clear",2:"Partly cloudy",3:"Overcast",45:"Fog",48:"Rime fog",51:"Light drizzle",53:"Drizzle",55:"Heavy drizzle",61:"Light rain",63:"Rain",65:"Heavy rain",71:"Light snow",73:"Snow",75:"Heavy snow",80:"Rain showers",81:"Showers",82:"Heavy showers",95:"Thunderstorm",96:"Thunderstorm + hail",99:"Strong thunderstorm + hail"})[c]||("Code "+c);}
+function renderLightningMini(){const el=$("lightningMini");if(!el)return;const st=lightningStats();
+  el.innerHTML=cfg.weatherbitKey?(S.lightningMeta?`⚡ ${st.total} in ${cfg.lightningMinutes} min / ${cfg.lightningRadius} km · ${st.nearest?(st.nearest.distanceKm*0.621371).toFixed(1)+" mi nearest":"none"}`:"Lightning loading…"):
+    `⚡ Point lightning off — add Weatherbit key in ⚙. NOAA GLM visual remains available in WEATHER.`;}
+function renderWeatherTab(B){const st=lightningStats(),m=S.meteo||{},obs=S.obs,H=S.wx&&S.wx.hourly,D=S.wx&&S.wx.daily;
+  let hourly="";if(H&&H.time){const nowH=new Date().toISOString().slice(0,13);let i=H.time.findIndex(t=>t.slice(0,13)>=nowH);if(i<0)i=0;
+    hourly=`<table><tr><th>Hour</th><th>Conditions</th><th>Temp</th><th>Precip</th><th>Wind / gust</th></tr>`+H.time.slice(i,i+12).map((t,j)=>{const k=i+j;return`<tr><td>${new Date(t).toLocaleTimeString([],{hour:"numeric"})}</td><td>${wxCodeLabel(H.weather_code&&H.weather_code[k])}</td><td>${Math.round(H.temperature_2m[k])}°F</td><td>${H.precipitation_probability[k]}%${H.precipitation?" · "+(+H.precipitation[k]).toFixed(2)+" in":""}</td><td>${H.wind_speed_10m?Math.round(H.wind_speed_10m[k]):"—"} / ${H.wind_gusts_10m?Math.round(H.wind_gusts_10m[k]):"—"} mph</td></tr>`;}).join("")+"</table>";}
+  const strikes=st.L.length?`<table><tr><th>Age</th><th>Distance</th><th>Source/type</th><th>Location</th></tr>`+st.L.slice(0,40).map(x=>`<tr data-fly="${x.lon},${x.lat},10" style="cursor:pointer"><td>◎ ${x.pastMins.toFixed(1)} min</td><td>${(x.distanceKm*0.621371).toFixed(1)} mi</td><td>${esc(x.source)} · ${esc(x.type)}</td><td>${x.lat.toFixed(3)}, ${x.lon.toFixed(3)}</td></tr>`).join("")+"</table>":`<div class="finer">${cfg.weatherbitKey?(S.lightningMeta?"No recent detections returned inside this radius/time window.":"Waiting for lightning fetch…"):"Point locations require a Weatherbit key and eligible plan. NOAA GLM visual cross-check below requires no key."}</div>`;
+  B.innerHTML=`<div style="font-size:14px;color:var(--vir3)">☁ Weather Core · ${esc(S.loc.name)}</div>
+  <div class="wxgrid"><div class="wxcard"><div class="finer">MODEL NOWCAST</div><div class="big">${m.temperature_2m!=null?Math.round(m.temperature_2m)+"°F":"—"}</div><div>${m.weather_code!=null?wxCodeLabel(m.weather_code):""} · feels ${m.apparent_temperature!=null?Math.round(m.apparent_temperature)+"°":"—"}</div><div class="finer">Open-Meteo · MODELED</div></div>
+  <div class="wxcard"><div class="finer">NEAREST STATION</div><div class="big">${obs&&obs.t!=null?Math.round(obs.t*9/5+32)+"°F":"—"}</div><div>${obs?esc(obs.txt||obs.station||""):"US observation unavailable"}</div><div class="finer">NWS/ASOS · MEASURED</div></div>
+  <div class="wxcard"><div class="finer">ACTIVE ALERTS HERE</div><div class="big">${S.alerts.filter(a=>a.local).length}</div><div>${S.alerts.filter(a=>a.local).slice(0,1).map(a=>esc(a.event)).join("")||"none active"}</div><div class="finer">NWS · OFFICIAL</div></div>
+  <div class="wxcard"><div class="finer">LIGHTNING WINDOW</div><div class="big">${cfg.weatherbitKey?(S.lightningMeta?st.total:"…"):"KEY"}</div><div>${st.nearest?`${(st.nearest.distanceKm*0.621371).toFixed(1)} mi nearest · ${st.nearest.pastMins.toFixed(1)} min ago`:cfg.weatherbitKey?"none returned":"optional keyed feed"}</div><div class="finer">${st.activity} · ${st.range}</div></div></div>
+  <div class="wxbar"><button class="hbtn on" id="wxRadar">▶ Radar timeline</button><button class="hbtn" id="wxRefresh">↻ Refresh weather</button><button class="hbtn" id="wxGlm">⚡ NOAA GLM official viewer ↗</button>
+  <label class="finer">Lightning radius <select id="wxRadius">${[25,50,75].map(v=>`<option value="${v}" ${cfg.lightningRadius===v?"selected":""}>${v} km</option>`).join("")}</select></label>
+  <label class="finer">Lookback <select id="wxMinutes">${[15,30,45].map(v=>`<option value="${v}" ${cfg.lightningMinutes===v?"selected":""}>${v} min</option>`).join("")}</select></label></div>
+  <div class="wxlegend"><span style="--c:#ff496c">≤5 min</span><span style="--c:#ffb84d">6–15 min</span><span style="--c:#ffe56b">older in window</span><span>Dashed ring = query radius</span></div>
+  <div class="finer">Lightning points are flash/detection locations, not guaranteed cloud-to-ground strikes. Activity compares the latest 10 minutes with the prior 10; distance trend compares their median ranges when both samples are large enough. Neither is a storm-arrival prediction.</div>
+  <h2>Recent Lightning</h2>${strikes}<h2>Next 12 Hours <span class="badge b-modeled">MODELED</span></h2>${hourly||'<div class="finer">Forecast loading…</div>'}
+  <div class="finer" style="margin-top:6px">Radar: NOAA NEXRAD mosaic via IEM <span class="badge b-measured">MEASURED</span> · alerts: NWS <span class="badge b-official">OFFICIAL</span> · point lightning: Weatherbit/provider source retained <span class="badge b-measured">MEASURED</span> · forecast: Open-Meteo <span class="badge b-modeled">MODELED</span>.</div>`;
+  $("wxRadar").addEventListener("click",()=>{LAYERS.radar.on=true;applyLayerVisibility();renderLayerList();radarPlay(true);});
+  $("wxRefresh").addEventListener("click",()=>{runAdapter("alerts",true);runAdapter("meteo",true);runAdapter("nwsobs",true);runAdapter("lightning",true);});
+  $("wxGlm").addEventListener("click",()=>window.open("https://www.star.nesdis.noaa.gov/goes/conus_band.php?band=EXTENT3&length=24&sat=G19","_blank","noopener"));
+  $("wxRadius").addEventListener("change",e=>{cfg.lightningRadius=+e.target.value;store.set("lightningRadius",cfg.lightningRadius);gj("lightningzone");runAdapter("lightning",true);});
+  $("wxMinutes").addEventListener("change",e=>{cfg.lightningMinutes=+e.target.value;store.set("lightningMinutes",cfg.lightningMinutes);runAdapter("lightning",true);});}
+
 const REGISTRY=[
  // ---- POWER GRID ----
  {m:"grid",id:"eia930",n:"EIA Hourly Grid Monitor (EIA-930)",t:1,st:"LIVE (keyed)",u:"https://www.eia.gov/electricity/gridmonitor/",w:"Official but hourly + 1-2h lag. Aggregated BA level — not substation telemetry."},
@@ -1721,6 +1805,8 @@
  {m:"earth",id:"donki",n:"NASA DONKI event notifications",t:2,st:"LIVE (DEMO_KEY)",u:"https://kauai.ccmc.gsfc.nasa.gov/DONKI/",w:"Analyst-issued notices; DEMO_KEY rate-limits."},
  {m:"earth",id:"usgsmag",n:"USGS Geomagnetism observatories",t:2,st:"STUB",u:"https://www.usgs.gov/programs/geomagnetism/data",w:"US magnetometer web service exists; adapter slot reserved (response format/CORS verification pending — will not ship half-tested)."},
  {m:"earth",id:"intermagnet",n:"INTERMAGNET global observatories",t:2,st:"LINK",u:"https://www.intermagnet.org/",w:"Access method and licensing vary per station."},
+ {m:"earth",id:"weatherbit_lightning",n:"Weatherbit Current Lightning",t:3,st:"LIVE (keyed/plan-dependent)",u:"https://www.weatherbit.io/api/lightning-current",w:"Returns recent flash/detection locations around a point. Provider quota applies; satellite flashes may be in-cloud, not necessarily ground strikes."},
+ {m:"earth",id:"noaa_glm",n:"NOAA GOES-19 GLM Flash Extent Density",t:1,st:"LINK",u:"https://www.star.nesdis.noaa.gov/goes/conus_band.php?band=EXTENT3&length=24&sat=G19",w:"Official total-lightning visualization updated in short frames; density imagery is a visual cross-check, not individual ground-strike classification."},
  {m:"earth",id:"blitz",n:"Blitzortung live lightning",t:4,st:"LINK",u:"https://map.blitzortung.org/",w:"Community network; their data terms prohibit third-party apps from consuming the live feed — so this app links rather than takes. Coverage varies by region."},
  {m:"earth",id:"wwlln",n:"WWLLN global lightning",t:2,st:"LINK",u:"https://wwlln.net/",w:"Research network; data by arrangement, not open API. Lightning DRIVES Schumann resonance but lightning density ≠ Schumann amplitude."},
  {m:"earth",id:"wwllnhist",n:"NASA WWLLN thunder-hour climatology",t:2,st:"LINK",u:"https://www.earthdata.nasa.gov/data/catalog/ghrc-daac-wwllnmth-1",w:"Historical baseline dataset."},
@@ -1732,7 +1818,8 @@
 ];
 function renderTab(){
   const B=$("tabBody");
-  if(curTab==="alerts"){const list=S.alerts.filter(a=>a.local||a.sev==="Extreme"||a.sev==="Severe");
+  if(curTab==="weather"){renderWeatherTab(B);return;}
+  if(curTab==="alerts"){const list=S.alerts.filter(a=>a.local||a.home||a.sev==="Extreme"||a.sev==="Severe");
     const cl=alertClusters();
     const clBar=cl.length?`<div class="row" style="flex-wrap:wrap;gap:4px;margin-bottom:6px">
       ${cl.slice(0,6).map((c,i)=>`<button class="hbtn" data-cluster="${i}" title="fly to the weighted center and pulse every alert in it">◎ Cluster ${i+1} · ${c.pts.length} alert(s)</button>`).join("")}
@@ -1792,12 +1879,25 @@
       ledger("Journal","","entry added (TIER 7 — subjective)","OK","USER","kept separate from measured data");});}
   if(curTab==="sources")B.innerHTML=`<div class="finer" style="line-height:1.8">
     <b style="color:var(--vir2)">Attribution & integrity.</b><br>
-    Basemap © OpenStreetMap contributors (ODbL) · Radar: NOAA NEXRAD via Iowa Environmental Mesonet · Earthquakes: USGS · Weather alerts: National Weather Service · Weather nowcast/forecast: Open-Meteo (MODELED) · NWS station observations (MEASURED) · Space weather: NOAA SWPC + NASA DONKI · Fires: NASA FIRMS · ISS: WhereTheISS.at · Aircraft: OpenSky Network · Grid: US EIA-930 · Power infrastructure: OpenStreetMap via Overpass (ODbL) · Rendering: MapLibre GL (BSD-3, vendored v5.24, license file in repo).<br><br>
+    Basemap © OpenStreetMap contributors (ODbL) · Radar: NOAA NEXRAD via Iowa Environmental Mesonet · Lightning points: Weatherbit (optional keyed/plan-dependent) · Lightning visual cross-check: NOAA GOES GLM · Earthquakes: USGS · Weather alerts: National Weather Service · Weather nowcast/forecast: Open-Meteo (MODELED) · NWS station observations (MEASURED) · Space weather: NOAA SWPC + NASA DONKI · Fires: NASA FIRMS · ISS: WhereTheISS.at · Aircraft: OpenSky Network · Grid: US EIA-930 · Power infrastructure: OpenStreetMap via Overpass (ODbL) · Rendering: MapLibre GL (BSD-3, vendored v5.24, license file in repo).<br><br>
     Every fetch is logged in the Truth Ledger with source, time, status, confidence class, and known limits. No prediction is worded as certainty unless the source itself is an official alert. This app claims no private grid topology, no substation vulnerability, no non-public outage prediction, and contains no scanning or probing capability of any kind. Schumann resonance remains honestly unavailable: no reliable public API exists, and this app will not fake one.</div>`;}
 document.querySelectorAll(".tab").forEach(t=>t.addEventListener("click",e=>{
   if(e.target.closest(".ibtn"))return; // ⓘ inside a tab opens the explainer without switching tabs
-  document.querySelectorAll(".tab").forEach(x=>x.classList.remove("on"));t.classList.add("on");
-  curTab=t.dataset.tab;renderTab();}));
+  activateTab(t.dataset.tab);}));
+
+let weatherMode=false,weatherLayerSnapshot=null;
+function activateTab(name){document.querySelectorAll(".tab").forEach(t=>t.classList.toggle("on",t.dataset.tab===name));curTab=name;renderTab();}
+function setWeatherMode(on){weatherMode=!!on;document.body.classList.toggle("wxmode",weatherMode);$("btnWeather").classList.toggle("on",weatherMode);
+  if(weatherMode){weatherLayerSnapshot=Object.fromEntries(Object.entries(LAYERS).map(([k,L])=>[k,L.on]));
+    for(const k in LAYERS)LAYERS[k].on=["radar","lightning","alerts","counties"].includes(k);LAYERS.terminator.on=false;
+    activateTab("weather");if(innerWidth<=900){document.body.classList.add("m-bottom");document.body.classList.remove("m-left","m-right");}
+    runAdapter("alerts",true);runAdapter("meteo",true);runAdapter("nwsobs",true);runAdapter("lightning",true);radarPlay(true);
+    ledger("Weather Core","","focused weather mode entered","OK","OFFICIAL","unrelated layers temporarily hidden; prior layer state preserved");
+  }else{if(weatherLayerSnapshot)for(const k in weatherLayerSnapshot)if(LAYERS[k])LAYERS[k].on=weatherLayerSnapshot[k];weatherLayerSnapshot=null;radarPlay(false);
+    ledger("Weather Core","","weather mode exited; prior layers restored","OK","OFFICIAL","");}
+  applyLayerVisibility();renderLayerList();setTimeout(()=>map.resize(),80);}
+$("btnWeather").addEventListener("click",()=>setWeatherMode(!weatherMode));
+
 $("btnHelp").addEventListener("click",openHelpIndex);
 /* ---- phone nav: one sheet at a time; Map closes all ---- */
 document.querySelectorAll("[data-mnav]").forEach(b=>b.addEventListener("click",()=>{
@@ -1823,10 +1923,13 @@
    LOCATION + UI WIRES
    ===================================================================== */
 function setLocation(loc,fly=true){cancelFollowSafe("navigated to "+(loc.name||"location"));S.loc=loc;
+  // Clear location-scoped data immediately so the new radius cannot display the prior location's readings.
+  S.meteo=null;S.wx=null;S.obs=null;S.lightning=[];S.lightningMeta=null;S.fires=[];S.alerts=S.alerts.filter(a=>!a.local);
+  ["lightning","lightningzone","fires","alertpolys"].forEach(gj);renderAll();
   if(fly){if(aeOpen)aeGoTo(loc.lat,loc.lon,loc.z||8.6);
     else map.flyTo({center:[loc.lon,loc.lat],zoom:loc.z||8.6,duration:2600});}
   ledger("Location","",'focused: '+loc.name,"OK","OFFICIAL","");
-  runAdapter("alerts",true);runAdapter("meteo",true);runAdapter("firms",true);
+  gj("lightningzone");runAdapter("alerts",true);runAdapter("meteo",true);runAdapter("nwsobs",true);runAdapter("lightning",true);runAdapter("firms",true);
   setTimeout(()=>{runAnomalyEngine();renderAll();},1500);}
 async function doSearch(){const q=$("searchInput").value;if(!q)return;
   const loc=await geocode(q);
@@ -1841,7 +1944,7 @@
   if(S.integrity&&S.mock)applyMock(false);
   renderAnomalies();ledger("Integrity mode","",S.integrity?"ON — hiding mock/experimental/low-confidence":"off","OK","OFFICIAL","");});
 $("btnMock").addEventListener("click",()=>applyMock(!S.mock));
-$("btnRadarPlay").addEventListener("click",()=>{if(!LAYERS.radar.on){LAYERS.radar.on=true;applyLayerVisibility();renderLayerList();}radarPlay(true);});
+$("btnRadarPlay").addEventListener("click",()=>{if(!LAYERS.radar.on){LAYERS.radar.on=true;applyLayerVisibility();renderLayerList();}radarPlay(!radarPlaying);});
 $("btnRadarPause").addEventListener("click",()=>radarPlay(!radarPlaying));
 $("btnScanPower").addEventListener("click",scanPower);
 /* ============== KEY VAULT + BACKUP (export/import as ONE file) ============== */
@@ -1858,6 +1961,9 @@
  ["tomtomKey","TomTom — road traffic",
   "https://developer.tomtom.com/user/register",
   ["Open the link above and create a free account (email or Google/GitHub)","Verify your email, then sign in","You land on the Dashboard — ignore all the product pages","Left menu → 'API & SDK Keys' (or 'Keys'): a key named 'My first API key' was ALREADY created for you","Click the copy icon next to it, paste here, SAVE","That one key covers the Traffic tiles this app uses — you don't need to pick any product"]],
+ ["weatherbitKey","Weatherbit — recent lightning points",
+  "https://www.weatherbit.io/account/create",
+  ["Open the link and create/sign into a Weatherbit account","Confirm that your plan includes the Current Lightning API — access is plan-dependent","Copy your API key from the dashboard","Paste it here and SAVE; Sentinel polls only every 15 minutes to protect quota"]],
  ["aisKey","AISStream — live ships",
   "https://aisstream.io/authenticate",
   ["Open the link above","Click 'Sign in with GitHub' (your mindnummer account works)","Authorize, then on the dashboard press 'Create API Key'","Name it anything, copy the key shown, paste here, SAVE","Then turn on the Ships layer and look at any busy strait or port"]]];
@@ -1900,7 +2006,7 @@
 $("btnSettings").addEventListener("click",()=>{
   const custom=store.get("customKeys",[]);
   $("modal").innerHTML=`<h3>⚙ Key Vault &amp; Settings — keys are stored locally and sent only to the selected data provider when that adapter runs</h3>
-  ${KEYFIELDS.map(([k,lbl,url,steps])=>`<label>${lbl} — <a href="${url}" target="_blank" rel="noopener" style="color:var(--vir3)">get free key ↗</a></label>
+  ${KEYFIELDS.map(([k,lbl,url,steps])=>`<label>${lbl} — <a href="${url}" target="_blank" rel="noopener" style="color:var(--vir3)">get key ↗</a></label>
     <input id="set_${k}" value="${esc(cfg[k]||"")}">
     <details class="keyhow"><summary>How to get this key (${steps.length} steps)</summary><ol>${steps.map(s=>`<li>${s}</li>`).join("")}</ol></details>`).join("")}
   <label>Your own keys — add any API key you find, with your label. Stored, exported in backups, ready the moment an adapter uses it. <button class="ibtn" data-info="customkeys">i</button></label>
@@ -2021,7 +2127,7 @@
   if(!notifOn||S.mock||typeof Notification==="undefined"||Notification.permission!=="granted")return;
   const h=cfg.home;
   // 1. New official NWS alert at home
-  S.alerts.filter(a=>a.local&&!a.mock).forEach(a=>{const id="al:"+(a.headline||a.event);
+  S.alerts.filter(a=>a.home&&!a.mock).forEach(a=>{const id="al:"+(a.headline||a.event);
     if(!notified.has(id)){rememberNotified(id);notify(a.event||"NWS alert","OFFICIAL · "+(a.headline||a.area||""));}});
   // 2. Quake M4.5+ within ~300 mi (480 km) of home
   S.quakes.filter(q=>!q.mock&&q.mag>=4.5&&haversine(h.lat,h.lon,q.lat,q.lon)<=480).forEach(q=>{
@@ -2030,7 +2136,11 @@
   // 3. Strong geomagnetic storm
   if(S.kp&&S.kp.v>=6){const id="kp:"+new Date().toISOString().slice(0,13);
     if(!notified.has(id)){rememberNotified(id);notify("Geomagnetic storm Kp "+S.kp.v.toFixed(1),"Watch condition — possible GPS/HF effects. MEASURED (NOAA SWPC)");}}
-  // 4. Fire detection within ~50 mi (80 km) of home
+  // 4. Recent lightning within 10 mi of home (only when focused location is home, because this keyed query follows focus)
+  if(Math.abs(S.loc.lat-h.lat)<0.01&&Math.abs(S.loc.lon-h.lon)<0.01)S.lightning.filter(x=>!x.mock&&x.distanceKm<=16.1&&x.pastMins<=10).forEach(x=>{
+    const id="l:"+(x.id||x.t+":"+x.lat.toFixed(3)+","+x.lon.toFixed(3));if(!notified.has(id)){rememberNotified(id);
+      notify("Recent lightning near home",`${(x.distanceKm*0.621371).toFixed(1)} mi away · ${x.pastMins.toFixed(1)} min ago · ${x.source||"lightning provider"} · flash may be in-cloud`);}});
+  // 5. Fire detection within ~50 mi (80 km) of home
   S.fires.filter(f=>!f.mock&&haversine(h.lat,h.lon,f.lat,f.lon)<=80).forEach(f=>{
     const id="f:"+f.lat.toFixed(2)+","+f.lon.toFixed(2)+":"+(f.when||"");
     if(!notified.has(id)){rememberNotified(id);
@@ -2045,7 +2155,7 @@
   notifOn=on;store.set("notifOn",notifOn);$("btnNotif").classList.toggle("on",notifOn);}
 $("btnNotif").addEventListener("click",()=>setNotif(!notifOn));
 
-function renderAll(){renderBrief();renderAnomalies();renderTab();renderLayerList();renderBaMini();}
+function renderAll(){renderBrief();renderAnomalies();renderTab();renderLayerList();renderBaMini();renderLightningMini();}
 async function runAll(force){
   if(S.mock){renderAll();return;}
   await Promise.allSettled(Object.keys(ADAPTERS).map(id=>runAdapter(id,force)));
@@ -2056,7 +2166,7 @@
 setInterval(()=>runAll(false),20000);
 setInterval(()=>{if(map.getSource("night"))gj("night");},120000);
 /* boot */
-renderLayerList();renderTab();watchdogSweep();
+renderLayerList();renderTab();renderLightningMini();watchdogSweep();
 /* ---- Views: globe / polar top-down (azimuthal perspective, honestly derived from the globe) ---- */
 let viewMode="globe";
 /* ---- Shared orientation. Polar geometry: north at the view center points toward the pole,
@@ -2510,6 +2620,8 @@
     g.strokeStyle="#ff5a6e";g.lineWidth=2;g.beginPath();g.arc(x,y,Math.max(3,(q.mag||3)),0,7);g.stroke();});
   if(LAYERS.fires&&LAYERS.fires.on)S.fires.forEach(f=>{const[x,y]=aeXY(f.lat,f.lon,cx,cy,R);if(!vis(x,y))return;
     g.fillStyle="#e07a4b";g.fillRect(x-2,y-2,4,4);});
+  if(LAYERS.lightning&&LAYERS.lightning.on)S.lightning.forEach(x=>{const[px,py]=aeXY(x.lat,x.lon,cx,cy,R);if(!vis(px,py))return;
+    g.fillStyle=x.pastMins<=5?"#ff496c":x.pastMins<=15?"#ffb84d":"#ffe56b";g.beginPath();g.arc(px,py,3.2,0,7);g.fill();});
   if(LAYERS.alerts&&LAYERS.alerts.on)alertPoints().forEach(a=>{const[x,y]=aeXY(a.lat,a.lon,cx,cy,R);if(!vis(x,y))return;
     g.fillStyle="#e0b24b";g.beginPath();g.moveTo(x,y-5);g.lineTo(x+5,y+4);g.lineTo(x-5,y+4);g.closePath();g.fill();});
   if(LAYERS.ba&&LAYERS.ba.on)Object.values(S.ba).forEach(b=>{if(!b.lon)return;const[x,y]=aeXY(b.lat,b.lon,cx,cy,R);if(!vis(x,y))return;
