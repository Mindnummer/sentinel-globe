# Tests

Run `python tests/static_audit.py` from the repository root. It checks HTML identity integrity, invalid nested controls, help-topic references, local assets, manifest icons, GeoJSON structure, and JavaScript syntax when Node is installed.

This is not a substitute for `DEPLOYMENT_CHECKLIST.md`, because external API/CORS behavior and MapLibre rendering require a real browser after deployment.
